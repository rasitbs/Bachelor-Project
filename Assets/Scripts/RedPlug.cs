using UnityEngine;

public class RedPlug : MonoBehaviour
{
    public bool isRedFromConnection = false;
    public bool isRedToConnection = false;
    public bool isBlackFromConnection = false;
    public bool isBlackToConnection = false;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
